<h3>社員情報新規登録4/4</h3>

<h1>有給休暇付与情報登録フォーム</h1>
  
<%= form_with model: [@user, @grant], url: user_grants_path(@user), local: true do |form| %>

  <%= render "grants/form", grant: @grant %>

<% end %>

  <div>
    <%= link_to "一覧に戻る", users_path, class: "btn btn-outline-primary btn-sm" %>
  </div>